# Docker serves the frontend and Python API on this local port.
PORT=5174
# Empty disables administration. Generate a secret of at least 32 characters.
# python -c "import secrets; print(secrets.token_urlsafe(48))"
# Paste your own value here and save as .env. Never commit .env.
ADMIN_TOKEN=
